ADMIN_PASSWORD=Boss@1234

# Vercel Postgres (prisma-postgres-camel-flask) — paste from Vercel → Project → Storage → Postgres → .env.local
POSTGRES_URL=

# Vercel Blob (boss-aus-mern-blob) — paste from Vercel → Project → Storage → Blob → .env.local
BLOB_READ_WRITE_TOKEN=
DATABASE_URL=