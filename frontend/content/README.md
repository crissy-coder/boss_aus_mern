# CMS Content

Pages created or updated from the admin panel are stored here as JSON files.

- `content/pages/index.json` – list of page slugs and metadata
- `content/pages/<slug>.json` – full content for each page

You can edit these files by hand or via the admin panel at `/admin`.
