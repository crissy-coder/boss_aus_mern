# Copy this file to .env or .env.local and set your values.
# Next.js does NOT load .env.example — only .env and .env.local.

ADMIN_PASSWORD=your_admin_password

# MongoDB Atlas. If you get querySrv ECONNREFUSED, use mongodb:// (not mongodb+srv://) with :27017
MONGODB_URI=mongodb://user:password@cluster.mongodb.net:27017/?retryWrites=true&w=majority
# Optional: database name (default: boss_cms)
# MONGODB_DB_NAME=boss_cms
