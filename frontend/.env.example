# Copy this file to .env or .env.local and set your values.
# Next.js does NOT load .env.example — only .env and .env.local.

ADMIN_PASSWORD=your_admin_password

# Neon Postgres (CMS pages). From Vercel → Storage → Postgres or Neon dashboard.
DATABASE_URL=postgresql://neondb_owner:npg_2TAkqzGd4Cyp@ep-spring-wildflower-ainl7zpo-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require

# Vercel Blob (CMS media). From Vercel → Storage → Blob → .env.local.
BLOB_READ_WRITE_TOKEN=your_blob_read_write_token
